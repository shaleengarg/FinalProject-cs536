
cd shared_libs/
make
sudo make install
cd ..
./runtest.sh


please take a look at runtest.sh. it is a barebones script which does the following:
1. Sets PCAnonRatio
2. compiles btio size C
3. runs it with vtune io perf tool
4. zips the folder

you can remove things which are not desired before running runtest.sh
